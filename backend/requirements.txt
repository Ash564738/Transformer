# ============================================================
# DGA Transformer Monitoring – Backend Requirements
# ============================================================
# Cài đặt: pip install -r requirements.txt
#
# Các gói được chia thành 3 nhóm:
#   [core]      : Flask API, xử lý dữ liệu, machine learning cơ bản
#   [training]  : Huấn luyện model (LightGBM, XGBoost, CatBoost, Temporal)
#   [optional]  : Snorkel (weak supervision), SHAP (giải thích model)
# ============================================================

# ── Core ──────────────────────────────────────────────────
flask
joblib
matplotlib
numpy
openai             # OpenAI SDK (dùng với OpenRouter cho chatbot)
openpyxl           # Đọc file Excel
pandas
python-dotenv      # Nạp biến môi trường từ .env
scikit-learn
scipy
seaborn            # Vẽ biểu đồ nâng cao (heatmap, barplot)

# ── Training pipeline ─────────────────────────────────────
catboost           # CatBoost classifier (supervised_experiment.py)
imbalanced-learn   # SMOTE và các kỹ thuật cân bằng dữ liệu
lightgbm
torch 
xgboost

# ── Optional but recommended ──────────────────────────────
lifelines          # (nếu có dùng trong survival analysis)
shap               # Giải thích model (SHAP values)
snorkel            # Weak supervision (Snorkel LabelModel)

# ── Dev / utilities ───────────────────────────────────────
# statsmodels      # Phân tích thống kê (bỏ comment nếu cần)