# Single requirements file for the whole backend: the live dashboard API
# (app.py -> /predict, /chat, /chart/*, /auth/*) and the offline training
# pipeline (train_models.py). Installing this once covers both.
numpy
pandas
matplotlib
openpyxl
flask
python-dotenv
openai  # OpenAI-compatible SDK, pointed at OpenRouter (text2sql_chat.py) — see README "DGA Assistant chatbot"
scikit-learn
lightgbm
xgboost
lifelines
torch
joblib
scipy
shap
